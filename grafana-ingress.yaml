apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-grafana
  namespace: cattle-monitoring-system
spec:
  rules:
  - host: graf.csdemo.csk8s.ch
    http:
      paths:
      - backend:
          service:
            name: rancher-monitoring-grafana
            port:
              number: 80
        path: /
        pathType: Prefix
